# 阅读控制台字号优化实施报告

## 交付范围

分支 `codex/extension-dashboard`，基于 `a0876c4`。仅修改 options 样式、进度条展示结构，以及本次测试和中文文档。主工作区 release、数据库、权限、Provider 与翻译业务逻辑均未修改。

- 正文与输入/选择/按钮默认 16px；辅助文案 14px；历史和分组标题 18px；页面标题 40px，窄屏 32px。
- 全部字号使用相对单位；显式覆盖扩展页面默认 body 小字号，增加中文系统字体回退。
- 辅助文字和辅助蓝色标题加深，实际渲染文字对比度检查至少 4.5:1（不包含禁用控件与装饰）。
- 长历史标题和域名换行；进度页码脱离绝对定位；侧栏在较窄窗口改为换行导航；表单网格根据空间换行。
- 按钮最小高度 2.75rem；保存操作栏进入正常文档流，不再悬浮遮挡内容。

## 验证结果（2026-09-04）

最终代码上的命令均退出 0：

| 命令 | 结果 |
| --- | --- |
| `npm test -- tests/unit/settings` | 6 个文件、34 个测试通过，Vitest 1.55 秒 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，WXT 4.991 秒 |
| `npm run verify:output` | 通过；Unicode noncharacter=0，静态 host 权限=0，静态 content script=0，options 独立标签页=1 |
| `npm run test:e2e -- dashboard-typography.spec.ts --max-failures=1` | 9 个测试通过，14.2 秒；全部 9 项执行，没有提前停止 |

定向测试先在旧 CSS 上确认两项预期失败；验证过程中发现并修正扩展默认 body 字号、中文标题行高及辅助蓝文字对比度。测试也对齐了既有“状态：”文案及原生必填校验，未改变这些业务行为。

E2E 使用真实生产构建扩展、全新临时 Chromium profile、两条虚构历史、默认空配置。8 个布局用例覆盖 4 档宽度 × 2 档文本缩放，每项覆盖五个分区，输出 40 张截图；另外 1 项检查搜索、类型筛选、空状态、键盘焦点、表单编辑、原生必填与应用内错误提示。未使用真实凭据、未请求外部模型服务。

## 截图与复核

截图根目录：本 worktree 的 `web-translate-plugin/test-results/`（生成产物，不提交 Git）。每个布局目录命名 `dashboard-typography-{宽度}px-{百分比}-文本缩放：五分区可读且无溢出`，内部文件为 `{分区}-{宽度}-text-{百分比}.png`。

已目视检查桌面历史与 AI 服务、1024px 存储、768px/200% 翻译偏好和 375px/200% PDF 解析。放大状态允许纵向滚动，不缩小文字、不截断说明；输入框中的超长值使用原生内部滚动。

## release 交接与限制

- 请按普通 merge 合入 release，由 release 会话执行一次独立复核、全量门禁和集成构建。
- 继续加载固定 release 产物并重载原扩展，不卸载、不清配置、不用此 feature 产物覆盖验收目录。
- 200% 根字号是文本缩放模拟，不是 Chrome 原生页面缩放。真实原生缩放、保存时原生授权与用户现有配置保留由 release 人工验收。
- 本次没有新增存储升级，也没有把 release 的数据库整合回灌 feature。该 feature 旧数据库基线只在隔离浏览器测试，不可覆盖当前用户验收插件。
- 未运行完整 `npm run check` 或全量 E2E，未额外启动 reviewer，避免与 release 控制器重复验证。
