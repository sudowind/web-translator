# 翻译语言字段对齐修复

功能提交 `472e143`，release 代码提交 `906763e`。原 `.field + .field` 给网格内第二个字段增加 18px 顶部间距，导致两列错位。新增 `.field-grid > .field { margin-top: 0; }`，网格间距由 gap 统一控制，网格外纵向表单间距不变。

独立只读复核无阻断问题。feature 定向样式测试 2 项通过，类型检查和构建通过。

最终验证在 release `906763e` 执行：

- `npm run check`：78 文件、534 项测试通过，类型检查、构建和产物检查通过。Vitest 20.20 秒，WXT 构建 3.439 秒。
- `npm run test:e2e -- dashboard-typography.spec.ts`：9 项通过，15.6 秒，覆盖 1440／1024／768／375px、100%／200% 文本缩放、双列输入框对齐、单列无重叠及表单操作。
- 已查看桌面与窄屏翻译偏好截图，确认布局正常。

主工作区保持 release，固定插件目录已构建；本报告为验证后的纯文档补充。用户重新加载原扩展并刷新设置页即可人工验收。浏览器测试使用隔离 profile，不更改用户配置。
