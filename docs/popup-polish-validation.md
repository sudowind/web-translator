# 阅读弹窗产品化验收

- 需求：优化弹窗内容与层级，突出阅读操作，默认收起开发诊断。
- 设计：深蓝文字 `#17243b`、主操作蓝 `#2854dc`、浅蓝底 `#f3f6fb`、白色阅读卡、分隔线 `#dbe3f0`；正文使用系统无衬线字体，品牌“译”字使用衬线字体。以阅读卡、控制台入口、折叠诊断组成三级结构。
- PDF 与网页分别显示“PDF 对照阅读”“网页对照翻译”，精简说明；诊断使用原生 details/summary，保留键盘操作。产品页标题改为 Web Translate。
- 功能分支提交 `034e086`，旧界面断言更新 `25b665e`；最终 release 验证提交 `fb2aca4`。合并冲突仅选择新版网页展示文案，保留 release 的流式进度和翻译启停逻辑。
- 独立只读复核无 Critical/Important；Minor 页面标题问题已修复。
- 最终 `npm run check`：556 项测试通过；类型检查、生产构建和产物验证通过。Vitest 15.35 秒，WXT 构建 2.829 秒。
- `npx playwright test popup-presentation.spec.ts`：2 项通过，4.5 秒。真实构建 UI 使用固定 PDF/网页状态，验证主入口、默认高度不超过 600px、无横向溢出、键盘展开和收起诊断；已查看两种状态截图。
- 该 UI 验证不代表原生 action Popup、activeTab 或原生站点权限验收。固定验收目录已从 release 构建，用户重载原扩展即可验收；没有卸载或清空配置。
