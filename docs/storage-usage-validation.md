# 本地数据总体积显示验收记录

- 功能提交：`8554a6c`；release 集成提交：`a88098a`。
- 存储与隐私页面增加“本地数据总占用（估算）”，不增加按篇明细。
- 使用扩展来源的 `navigator.storage.estimate().usage`，涵盖本地数据库存储；并非原始 PDF 文件体积，不扫描或序列化全库。读取失败或不可用显示“暂不可用”，真实零值显示 `0 B`。
- 页面加载及已有清理操作完成后更新；浏览器估算可能延迟，本次不增加实时轮询。
- 独立只读复核无阻断问题。
- 集成提交上 `npm run check` 通过：81 个测试文件、556 项测试，类型检查、生产构建及产物检查通过。Vitest 24.87 秒，WXT 构建 3.958 秒；测试阶段为主要耗时。
- `npx playwright test dashboard-typography.spec.ts`：9 项通过，15.6 秒。隔离 Chromium 加载真实扩展产物，覆盖 375/768/1024/1440 宽度与 100%/200% 根字号；总体积返回数值，布局无溢出。已查看桌面截图。
- 固定验收目录已从 release 构建；用户原 Chrome 扩展仍需手动重载，本次没有读取、清空或迁移用户数据库。
